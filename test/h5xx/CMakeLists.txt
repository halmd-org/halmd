foreach(module
    ctype
    error
    )
  add_executable(test_h5xx_${module}
    ${module}.cpp
    )
  target_link_libraries(test_h5xx_${module}
    ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY}
    ${HDF5_LIBRARY}
    )
  add_test(h5xx_${module}
    test_h5xx_${module}
    )
endforeach(module)
