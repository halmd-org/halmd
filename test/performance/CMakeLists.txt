if(WITH_CUDA)
  add_executable(test_callbacks
    callbacks.cpp
    callbacks_extern.cpp
    callbacks_extern_kernel.cu
  )
else(WITH_CUDA)
  add_executable(test_callbacks
    callbacks.cpp
    callbacks_extern.cpp
    callbacks_extern_kernel.cu
  )
endif(WITH_CUDA)
target_link_libraries(test_callbacks
  test_tools_lua
  ${HALMD_COMMON_LIBRARIES}
  ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY}
)
add_test(callbacks
  test_callbacks --log_level=message
)
