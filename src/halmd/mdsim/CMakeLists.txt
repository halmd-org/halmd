add_library(halmd_mdsim
  box.cpp
  core.cpp
  force.cpp
  integrator.cpp
  neighbor.cpp
  particle.cpp
  position.cpp
  random.cpp
  sort.cpp
  velocity.cpp
  )
if(WITH_CUDA)
  target_link_libraries(halmd_mdsim
    halmd_mdsim_gpu
    )
endif(WITH_CUDA)
target_link_libraries(halmd_mdsim
  halmd_mdsim_host
  halmd_mdsim_samples_host
  )

add_subdirectory(backend)
if(WITH_CUDA)
  add_subdirectory(gpu)
endif(WITH_CUDA)
add_subdirectory(host)
add_subdirectory(samples)
