if(WITH_CUDA)
  foreach(test
      add
      div
      mul
      sqrt
      mulss
      sub
      )
    add_executable(test_dsfun_${test}
      ${test}.cpp
      ${test}_kernel.cu
      )
    target_link_libraries(test_dsfun_${test}
      ${CUDA_LIBRARIES}
      pthread
      )
    add_test(dsfun_${test} test_dsfun_${test})
  endforeach(test)
endif(WITH_CUDA)
