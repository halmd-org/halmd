add_library(sample
  H5param.cpp
  correlation.cpp
  energy.cpp
  perf.cpp
  trajectory.cpp
  )
if(WITH_CUDA)
  target_link_libraries(sample
    ${CUDA_LIBRARIES}
    gpu_tcf
    )
  add_subdirectory(gpu)
endif(WITH_CUDA)
