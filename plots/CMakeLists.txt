FOREACH(fn trajectories2d.gpi trajectories3d.gpi gnuplot_movie)
  CONFIGURE_FILE("${CMAKE_CURRENT_SOURCE_DIR}/${fn}" "${CMAKE_CURRENT_BINARY_DIR}/${fn}" @ONLY)
ENDFOREACH(fn)

FILE(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/data)
